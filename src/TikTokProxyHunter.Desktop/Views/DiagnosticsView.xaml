<UserControl x:Class="TikTokProxyHunter.Desktop.Views.DiagnosticsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <Grid Margin="30"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><DockPanel><TextBlock Text="Диагностика" Style="{StaticResource PageTitle}"/><StackPanel DockPanel.Dock="Right" Orientation="Horizontal"><Button Content="Проверить всё" Command="{Binding RefreshCommand}" Margin="0,0,8,0"/><Button Content="Скопировать отчёт" Command="{Binding CopyReportCommand}"/></StackPanel></DockPanel><Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,18,0,10"><WrapPanel><TextBlock Text="Версия: "/><TextBlock Text="{Binding ApplicationVersion}" FontWeight="SemiBold" Margin="0,0,20,0"/><TextBlock Text=".NET: "/><TextBlock Text="{Binding RuntimeVersion}" FontWeight="SemiBold" Margin="0,0,20,0"/><TextBlock Text="Источников включено: "/><TextBlock Text="{Binding EnabledSources}" FontWeight="SemiBold"/></WrapPanel></Border><ItemsControl Grid.Row="2" ItemsSource="{Binding Components}" MaxHeight="220"><ItemsControl.ItemTemplate><DataTemplate><Border Style="{StaticResource Card}" Padding="10" Margin="0,0,0,5"><DockPanel><TextBlock Text="{Binding Name}" FontWeight="SemiBold" Width="210"/><TextBlock Text="{Binding Status}" Width="160"/><TextBlock Text="{Binding Detail}" Style="{StaticResource SecondaryText}" TextTrimming="CharacterEllipsis"/></DockPanel></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl><Grid Grid.Row="3" Margin="0,14,0,0"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><DockPanel><TextBlock Text="Журнал" Style="{StaticResource SectionTitle}"/><Button DockPanel.Dock="Right" Content="Debug" Command="{Binding ShowDebugCommand}"/></DockPanel><ListBox Grid.Row="1" ItemsSource="{Binding LogLines}" FontFamily="Consolas" FontSize="12" Background="{DynamicResource InputBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" Margin="0,8,0,0" VirtualizingPanel.IsVirtualizing="True"/></Grid></Grid>
</UserControl>
